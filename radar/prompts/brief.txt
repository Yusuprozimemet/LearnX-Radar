You are writing a short teaching brief that will be turned into a spoken audio
lesson for a working developer. The topic is a skill that surfaced in the
developer world today.

SKILL: {skill}
WHY IT SURFACED: {evidence}
SEEN IN: {sources}
{prior_context}
GROUNDING SOURCES (real material collected today). Base your facts on these —
prefer their concrete details, versions, names, and examples over generic
knowledge, and do NOT state specifics they don't support. Cite them inline as [n]
using their numbers where a claim rests on one. NEVER invent a URL or a source not
listed here, and do NOT write a Sources list yourself — it is appended for you:
{grounding}

INSIGHT BAR — this is what separates a real briefing from an encyclopedia entry:
name the SPECIFIC tools, projects, techniques, people, and debates the sources are
actually talking about (concrete library/product names, named workflows or
patterns, competing approaches and the tension between them), not just the abstract
concept. Prefer what is NEW or CONTESTED right now over textbook definitions a
reader could get anywhere. If the sources mention specific named things, the brief
MUST surface them.

Write the brief in Markdown, roughly 600-800 words, with EXACTLY these sections:

# {skill}

## Why it's on the radar today
Two or three sentences grounding the skill in why it showed up now (use the
evidence and the sources it appeared in). Concrete, no hype.

## What's being discussed
3-5 sentences (or tight bullets) on the SPECIFIC current conversation: the named
tools, projects, techniques, and disagreements practitioners are weighing right
now, drawn from the grounding sources with [n] citations. This is where the brief
earns its keep — be concrete and current, name names, surface the live tensions.
If the sources genuinely offer no specifics, say briefly what's driving interest
instead of padding with generalities.

## What you'll learn
A bullet list of 3-5 concrete things the lesson will cover.

## Core ideas
The actual teaching content: explain the key concepts in clear prose, grounded in
the specific tools and techniques named above (not a generic overview), with one
or two short, correct examples. This is the heart of the lesson — make it
genuinely instructive, not a summary.

## Where it fits
How this connects to the broader stack and when a developer would reach for it.

## Do this in 5 minutes
ONE concrete, runnable exercise the reader can do right now to get hands-on — a
command to run, a tiny code snippet to type and execute, or one specific repo or
doc page to open and skim. Be specific and immediately actionable; include a short
code block when the skill is code-shaped. No "explore the ecosystem" filler, no
multi-step project — just one small thing they can finish in five minutes.

Do NOT write a "## Sources" section — it is appended automatically from the real
URLs. If no grounding sources were provided above, use no [n] markers anywhere.

Write only the Markdown brief. No preamble, no closing remarks.
